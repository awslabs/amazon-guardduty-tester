apiVersion: v1
kind: Pod
metadata:
  name: php-app
  labels:
    app: php-app
spec:
  containers:
  - name: php-apache
    image: ${IMAGE_NAME}
    ports:
    - containerPort: 80
    securityContext:
      privileged: true

